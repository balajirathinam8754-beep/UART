interface uart_if(input logic clk);

    logic       rst;

    logic       tx_start;
    logic [7:0] tx_data;

    logic       tx;
    logic       tx_busy;

    logic       rx;

    logic [7:0] rx_data;
    logic       rx_done;

endinterface
